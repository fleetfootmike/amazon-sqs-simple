#!/usr/bin/perl -w

use strict;

my $f = shift || '-';
my %h;

open(F, "$f") or die;
while (<F>) {
    /Sending (\d+)/  and $h{$1} = $_;
    /\[INFO\] (\d+)/ and delete $h{$1};
}
close F;

print sort values %h;

